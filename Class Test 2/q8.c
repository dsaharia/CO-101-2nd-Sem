/*8. Run a system command of Operating system and store the result of the command in a file (Use argc, *argv[] and any other necessary operations). (marks: 2)
*/
#include<stdio.h>
int main()
{
    system("touch dhiraj.txt"); // To create a file name dhiraj.txt
    return 0;
}
